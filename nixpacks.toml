[phases.setup]
nixPkgs = ["php84", "php84Packages.composer", "nodejs_20", "php84Extensions.gd", "php84Extensions.intl", "php84Extensions.pdo_mysql", "php84Extensions.mysqlnd", "php84Extensions.openssl", "php84Extensions.mbstring", "php84Extensions.curl", "openssl"]

[phases.install]
dependsOn = ["setup"]
cacheDirectories = ["/root/.composer/cache", "node_modules"]
cmds = ["composer install --no-dev --no-interaction --prefer-dist --optimize-autoloader", "npm ci"]

[phases.build]
dependsOn = ["install"]
cmds = ["npm run build", "php artisan optimize"]

[start]
cmd = "php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT"
